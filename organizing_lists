# Use the sort() method to sort lists alphabetically. This has a permanent effect on the list.

cars = ['bmw', 'audi', 'toyota', 'subaru']
cars.sort()
print(cars)

# Use sort(reverse=True) to sort a list in reverse alphabetical order. This has a permanent effect on the list.

cars.sort(reverse=True)
print(cars)

# Use the sorted() method to sort the list but maintain the original order.

cars = ['bmw', 'audi', 'toyota', 'subaru']
print(sorted(cars))
print(cars)

# Use reverse() to simply reverse the order of the list. Not alphabetically. This has a permanent effect on the list.

print('\n')
cars.reverse()
print(cars)

# Use the len() method to find the length of a list
print(len(cars))

# Activity provided below. Pg. 46.

# First bullet point
places = ['Washington D.C.', 'Santorini', 'Austin', 'Colorado', 'Singapore']
# Second bullet point
print(places)
# Third bullet point
print(sorted(places))
# Fourth bullet point
print(places)
# Fifth bullet point
print(sorted(places, reverse=True))
# Sixth bullet point
print(places)
# Seventh bullet point
places.reverse()
print(places)
# Eighth bullet point
places.reverse()
print(places)
# Nineth bullet point
places.sort()
print(places)
# Tenth bullet point
places.sort(reverse=True)
print(places)

